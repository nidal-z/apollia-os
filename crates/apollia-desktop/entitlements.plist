<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTD PropertyList-1.0.dtd">
<!--
  Apollia OS - hardened runtime entitlements.

  PyO3 embeds a Python interpreter that executes dynamically compiled bytecode
  and loads `libpython3.13.dylib` shipped from python-build-standalone (signed
  by Astral, not by our Team ID). These three exceptions are the minimum set
  required for the bundled Python to load under the hardened runtime.

  The app captures the microphone in-process (apollia-stt via cpal), so the
  hardened runtime also needs the audio-input resource entitlement; without it
  a Developer ID build loses the microphone that the ad hoc build had.
-->
<plist version="1.0">
<dict>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>com.apple.security.device.audio-input</key>
    <true/>
</dict>
</plist>
