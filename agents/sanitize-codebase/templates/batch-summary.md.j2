{# Notification template emitted at end of each batch run. #}
**Apollia OS sanitize batch {{ today }}**

Files seen this tick : {{ batch | length }}
- code_clean : {{ counts.code_clean }}
- comments_clean : {{ counts.comments_clean }}
- done : {{ counts.done }}
- residue : {{ counts.residue }}

Cumulative progress : {{ progress.done }} done / {{ progress.total }} discovered ({{ progress.percent }}%)

Wall clock : {{ wall_clock_secs }}s.
Tool calls : {{ tool_calls }}.

{% if residues %}
Residues this tick :
{% for r in residues %}
- `{{ r.path }}` : {{ r.last_reason }}
{% endfor %}
{% endif %}
