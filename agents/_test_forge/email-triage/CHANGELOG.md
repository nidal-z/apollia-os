# Changelog — email-triage

## [1.0.0] — 2026-04-30

### Added
- `OrchestratedAgent` avec `execution_mode="orchestrated"` (ORIA pilote)
- `tools_requiring_approval=["http_fetch"]` → HITL automatique avant chaque appel Gmail
- `system_prompt` riche guidant le Reasoner (workflow + règles)
- `on_plan_complete()` agrégation step_results → synthèse markdown
- Triggers : cron matinal `0 8 * * 1-5` + manuel
- Sections APOLLIA.md : Classification Rules, VIP List, Auto-Reply Templates
- Datasource exemple format Gmail API simplifié

### Known Gaps (stories candidates)
- Pas d'outil gmail natif Apollia v0.1 (wrappers http_fetch requis)
- `user.agents.hitl` pas lu dynamiquement par le runtime
- Pas de fan-out parallèle dans ORIA actuel

### Generated by
apollia-agent-forge skill v1
