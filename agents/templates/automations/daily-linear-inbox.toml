kind = "automation"
name = "Daily Linear inbox alert"

[source]
kind = "cron"
expression = "0 8 * * *"

[target]
agent = "dev-assistant"
prompt = "List the Linear issues newly assigned to me since yesterday morning, grouped by priority. Use the Linear MCP tools."
