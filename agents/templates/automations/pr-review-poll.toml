kind = "automation"
name = "Open PR watcher"

[source]
kind = "interval"
seconds = 1800

[target]
agent = "review-assistant"
prompt = "Check open pull requests on the primary repo. For each one waiting on my review, summarize the diff in 3 bullets and flag risky changes."
