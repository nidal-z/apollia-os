# Apollia OS capabilities (seed knowledge stub)

Short capabilities sheet bundled so the guide agent directory mirrors the real
bundled layout. Content is illustrative only.

- Run local Python agents in isolation with tools.
- Free chat with configurable system prompt, tools and LLM backend.
- Install agents and agent packages from disk.
- Persistent memory per agent namespace.
